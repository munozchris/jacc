From terminal: sudo pip3 install selenium

Chrome(What I did):

Download chrome in your VM

Download chromium webdriver in your VM: (https://sites.google.com/a/chromium.org/chromedriver/downloads)

Move chromium into usr/lib/ directory

Use: sudo mv webdriver-filename(or whatever its called) directory-path

Using Mozilla (must update to ver. 48):

sudo apt-get update
sudo apt-get install firefox

Download Geckodriver:
https://github.com/mozilla/geckodriver/releases

sudo mv webdriver-filename(whatever its called) directory-path